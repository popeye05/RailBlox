import {useState,type ReactNode} from 'react';
import {authenticatedFetch,API} from './api';
export function DownloadLink({href,children,className,download:_download}:{href:string;children:ReactNode;className?:string;download?:boolean}){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <><a className={className} href={href} aria-disabled={busy} onClick={async e=>{e.preventDefault();if(busy)return;setBusy(true);setError('');try{if(!href.startsWith(API+'/'))throw Error('Invalid export target');const response=await authenticatedFetch(href.slice(API.length));if(!response.ok){const data=await response.json();throw Error(data.message||'Export failed')}const blob=await response.blob(),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=response.headers.get('Content-Disposition')?.match(/filename="([^"]+)"/)?.[1]||'railblox-export.json';link.click();window.setTimeout(()=>URL.revokeObjectURL(url),1000)}catch(e){setError(String(e))}finally{setBusy(false)}}}>{busy?'Preparing download…':children}</a>{error&&<span role="alert" className="error">{error}</span>}</>}
